#!/bin/dash

# original shell command: zero=0
zero=0

# original shell command: while test $zero -lt 10
while test $zero -lt 10
do
# original shell command: do

    # original shell command: echo $zero
    echo $zero

    # original shell command: zero=$((zero+1))
    zero=$((zero+1))

# original shell command: done
done
