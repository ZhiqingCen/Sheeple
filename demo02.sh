#!/bin/dash

# original shell command: if [ 1 -lt 10 ]
if [ 1 -lt 10 ]
then
# original shell command: then

    # original shell command: echo succeed
    echo succeed

# original shell command: fi
fi
